import React from 'react';

function PlaceholderComponent({ filename }) {
  return (
    <span>
      Puedes editar este componente en <b>{filename}</b>
    </span>
  );
}

export default PlaceholderComponent;
